openapi: 3.0.3
info:
  title: COOKie Identity Public API
  version: 0.6.0
  description: >-
    Public email/password authentication and session lifecycle owned by
    Identity Service.
servers:
  - url: https://api.cookie.app/v1
    description: Production
  - url: http://127.0.0.1:8080/v1
    description: Local
tags:
  - name: auth
    description: Email/password authentication and session lifecycle.

paths:
  /auth/email/register:
    post:
      tags: [auth]
      operationId: registerWithEmail
      summary: Зарегистрироваться по email и паролю
      description: >-
        Создаёт proof-bound попытку регистрации и запрашивает доставку
        одноразового verification token.
        Клиент до запроса генерирует и атомарно сохраняет стабильный registrationAttemptId
        и независимый 256-битный registrationProof. Точный повтор того же запроса является
        no-op и после фиксации попытки не расходует email business bucket. Защитный IP bucket
        применяется ко всем запросам и поэтому повтор также может получить 429. Повтор того же attempt id или
        proof с изменённым email, password либо locale отклоняется конфликтом.
        Exact retry истёкшей или проигравшей конкуренцию попытки также остаётся
        no-op, пока её scrubbed tombstone хранится не менее 30 дней; чтобы получить
        новое письмо после 24 часов, клиент всегда создаёт новый id и proof.
        Email token и registrationProof по отдельности не могут активировать credential. Ответ
        одинаков для нового и уже зарегистрированного email. Сессия выдаётся только
        отдельным login после подтверждения.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EmailRegistrationRequest"
      responses:
        "202":
          description: Если действие допустимо, письмо подтверждения будет отправлено
        "400":
          $ref: "#/components/responses/BadRequest"
        "409":
          $ref: "#/components/responses/RegistrationAttemptConflict"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "429":
          $ref: "#/components/responses/TooManyRequests"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /auth/email/login:
    post:
      tags: [auth]
      operationId: loginWithEmail
      summary: Войти по email и паролю
      description: >-
        Возвращает одинаковую ошибку для неизвестного email, неверного пароля,
        неподтверждённого email и недоступного аккаунта, чтобы не позволять
        перечислять зарегистрированные адреса.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EmailLoginRequest"
      responses:
        "200":
          description: Токены выданы, refresh-сессия создана
          headers:
            Cache-Control:
              description: Запрещает хранение ответа с bearer credentials
              schema:
                type: string
                example: no-store
            Pragma:
              description: Совместимый с HTTP/1.0 запрет кэширования
              schema:
                type: string
                example: no-cache
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TokenPairWithUser"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          description: Неверные учётные данные
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "429":
          $ref: "#/components/responses/TooManyRequests"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /auth/email/verification/confirm:
    post:
      tags: [auth]
      operationId: confirmEmail
      summary: Подтвердить email одноразовым токеном
      description: >-
        Атомарно проверяет доставленный по email verification token и независимый
        client-held registrationProof, после чего активирует связанный с ними password hash.
        Token содержит публичный registrationAttemptId, по которому клиент выбирает proof
        из защищённого локального хранилища; сам proof не включается ни в email payload,
        ни в будущий deep link.
        Повтор уже успешно погашенного токена в течение как минимум 30 дней является
        безопасным no-op; после удаления audit-записи токен снова считается недействительным. Сессия
        создаётся отдельным login-запросом, поэтому неопределённый результат HTTP
        confirm не может потерять выданный refresh secret.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EmailVerificationConfirmRequest"
      responses:
        "204":
          description: Email подтверждён или audit-запись уже фиксирует успешное погашение этого токена
        "400":
          description: Token/proof pair недействительна, погашена другим запросом или истекла
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "429":
          $ref: "#/components/responses/TooManyRequests"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /auth/email/verification/resend:
    post:
      tags: [auth]
      operationId: resendEmailVerification
      summary: Повторно отправить подтверждение email
      description: >-
        Всегда возвращает одинаковый ответ независимо от существования аккаунта и
        состояния email. Письмо повторно отправляется только для указанной попытки после проверки
        client-held registrationProof. Попытка живёт 24 часа независимо от cleanup, отдельный token —
        30 минут. Ранее выданные для этой же попытки неизрасходованные tokens остаются пригодны
        до своего expiry независимо от формата доставки; подтверждение одной попытки делает
        остальные бесполезными.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EmailActionRequest"
      responses:
        "202":
          description: Если действие допустимо, письмо будет отправлено
        "400":
          $ref: "#/components/responses/BadRequest"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "429":
          $ref: "#/components/responses/TooManyRequests"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /auth/refresh:
    post:
      tags: [auth]
      operationId: refreshSession
      summary: Обновить пару токенов (refresh token rotation)
      description: >-
        Однократно использует текущий refresh credential и возвращает новую пару.
        Для повтора того же логического HTTP-запроса клиент обязан повторить
        тот же Idempotency-Key с тем же refresh token. Пока непосредственный
        successor остаётся текущим, а family активна и не истекла, Identity
        детерминированно вернёт тот же successor refresh token. После следующей
        rotation точный retry становится stale и отклоняется без отзыва сессии.
        Другой ключ для уже использованного credential считается reuse и атомарно
        отзывает всё семейство refresh-сессии.
      security: []
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          description: >-
            Криптографически случайный RFC 4122 UUIDv4 одной логической попытки
            refresh. Новый UUIDv4 создаётся для каждой rotation и повторяется
            только при сетевом retry того же refresh token. Пара refresh token +
            Idempotency-Key является bearer-sensitive recovery material и не
            должна попадать в логи.
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RefreshTokenRequest"
      responses:
        "200":
          description: Новая пара токенов
          headers:
            Cache-Control:
              description: Запрещает хранение ответа с bearer credentials
              schema:
                type: string
                example: no-store
            Pragma:
              description: Совместимый с HTTP/1.0 запрет кэширования
              schema:
                type: string
                example: no-cache
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TokenPair"
        "401":
          description: Refresh token недействителен, отозван, уже использован или истёк
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "400":
          $ref: "#/components/responses/BadRequest"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "429":
          $ref: "#/components/responses/TooManyRequests"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /auth/logout:
    post:
      tags: [auth]
      operationId: logout
      summary: Завершить одну логическую сессию устройства
      description: >-
        Любой криптографически валидный refresh token семейства аутентифицирует
        логическую сессию устройства; Identity отзывает всё семейство, включая
        токен-замену, созданный конкурентным refresh.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RefreshTokenRequest"
      responses:
        "204":
          description: Сессия завершена или уже была завершена
        "400":
          $ref: "#/components/responses/BadRequest"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "429":
          $ref: "#/components/responses/TooManyRequests"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /auth/jwks:
    get:
      tags: [auth]
      operationId: getIdentityJwks
      summary: Получить публичные ключи подписи access JWT
      description: >-
        Возвращает активный и retiring ES256 ключи для локальной проверки access JWT.
      security: []
      responses:
        "200":
          description: JSON Web Key Set
          headers:
            Cache-Control:
              schema:
                type: string
              example: public, max-age=300
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JsonWebKeySet"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

components:
  responses:
    BadRequest:
      description: Ошибка валидации
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    PayloadTooLarge:
      description: Тело запроса превышает серверный лимит 16 KiB
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    TooManyRequests:
      description: Слишком много запросов; повторите позднее
      headers:
        Retry-After:
          description: Число секунд до следующей разрешённой попытки
          schema:
            type: integer
            minimum: 1
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    ServiceUnavailable:
      description: Сервис временно не готов обрабатывать запросы
      headers:
        Retry-After:
          description: Рекомендуемая задержка перед повтором, в секундах
          schema:
            type: integer
            minimum: 1
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    RegistrationAttemptConflict:
      description: Attempt id или registration proof уже связан с другим запросом
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"

  schemas:
    EmailAddress:
      type: string
      format: email
      description: >-
        Структурно корректный email с ASCII local-part и Unicode- или ASCII-доменом.
        Перед сравнением пробелы по краям удаляются, домен проходит
        non-transitional UTS #46 IDNA-преобразование, а весь адрес приводится к
        нижнему регистру. Лимит 254 символа проверяется для канонической ASCII-формы,
        а не для исходной JSON-строки. Ограничение новых регистраций доменами .ru и .рф не
        применяется к login/resend, чтобы сохранённые credentials оставались
        доступны после изменения admission policy.

    RegistrationEmailAddress:
      type: string
      format: email
      description: >-
        Email для новой регистрации: ASCII local-part, Unicode- или ASCII-домен
        и максимум 254 символа в канонической ASCII-форме после IDNA-преобразования.
        Итоговый домен верхнего
        уровня должен быть .ru или .рф (xn--p1ai).

    Password:
      type: string
      format: password
      writeOnly: true
      description: >-
        От 15 до 128 Unicode code points после NFC-нормализации. Обычные пробелы,
        Unicode и emoji разрешены; управляющие символы и разделители строк и
        абзацев запрещены. Требования к составу не навязываются. На сервере
        хранится только Argon2id encoded hash нормализованного значения.

    LoginPassword:
      type: string
      format: password
      writeOnly: true
      description: >-
        Пароль для проверки существующего credential. Текущая registration
        policy намеренно не применяется при login, чтобы её ужесточение не
        блокировало ранее созданные аккаунты. Перед проверкой применяется та же
        NFC-нормализация и безопасный максимум, что и при регистрации.

    RegistrationProof:
      type: string
      minLength: 43
      maxLength: 43
      pattern: "^[A-Za-z0-9_-]{43}$"
      writeOnly: true
      description: >-
        Base64url без padding от 32 криптографически случайных байтов. Клиент
        генерирует новое значение для каждой логической регистрации, хранит его как secret
        и повторяет в resend/confirm. Identity хранит только SHA-256 verifier.

    RegistrationAttemptId:
      type: string
      format: uuid
      description: >-
        Криптографически случайный UUID одной логической регистрации. Клиент создаёт
        его вместе с registrationProof, хранит эту пару до завершения flow и повторяет
        идентификатор при register/resend. Он не является secret и встроен в email token,
        чтобы полученный через development handoff или будущий deep link token можно
        было однозначно сопоставить с сохранённым proof.

    LocaleTag:
      type: string
      maxLength: 255
      example: ru-RU
      description: >-
        Well-formed BCP 47 language tag. Сервер канонизирует регистр subtags.
        Ограничение 255 символов является защитным product/storage bound, а не
        ограничением стандарта BCP 47.

    EmailRegistrationRequest:
      type: object
      required: [registrationAttemptId, email, password, registrationProof]
      properties:
        registrationAttemptId:
          $ref: "#/components/schemas/RegistrationAttemptId"
        email:
          $ref: "#/components/schemas/RegistrationEmailAddress"
        password:
          $ref: "#/components/schemas/Password"
        registrationProof:
          $ref: "#/components/schemas/RegistrationProof"
        locale:
          $ref: "#/components/schemas/LocaleTag"

    EmailLoginRequest:
      type: object
      required: [email, password]
      properties:
        email:
          $ref: "#/components/schemas/EmailAddress"
        password:
          $ref: "#/components/schemas/LoginPassword"
        deviceId:
          type: string
          minLength: 1
          maxLength: 255
          description: >-
            Opaque id установки клиента; непустой, не blank и без control,
            format, line/paragraph separator или malformed surrogate code points.

    EmailActionRequest:
      type: object
      required: [registrationAttemptId, email, registrationProof]
      properties:
        registrationAttemptId:
          $ref: "#/components/schemas/RegistrationAttemptId"
        email:
          $ref: "#/components/schemas/EmailAddress"
        registrationProof:
          $ref: "#/components/schemas/RegistrationProof"

    EmailVerificationConfirmRequest:
      type: object
      required: [token, registrationProof]
      properties:
        token:
          type: string
          minLength: 121
          maxLength: 121
          description: >-
            Opaque email token, содержащий публичные attempt/token UUID и
            256-битный secret. Клиент не должен логировать или сохранять его вне flow.
        registrationProof:
          $ref: "#/components/schemas/RegistrationProof"

    RefreshTokenRequest:
      type: object
      required: [refreshToken]
      properties:
        refreshToken:
          type: string
          minLength: 32
          maxLength: 512

    TokenPair:
      type: object
      description: >-
        Access token — короткоживущий JWT и не хранится на сервере. Refresh token —
        256-битный opaque secret; клиент хранит его в Keychain/Keystore, а Identity
        сохраняет в PostgreSQL только verifier и состояние rotation family.
      required: [accessToken, accessTokenExpiresIn, refreshToken, refreshTokenExpiresIn]
      properties:
        accessToken:
          type: string
        accessTokenExpiresIn:
          type: integer
          description: seconds
          example: 900
        refreshToken:
          type: string
        refreshTokenExpiresIn:
          type: integer
          description: seconds

    TokenPairWithUser:
      allOf:
        - $ref: "#/components/schemas/TokenPair"
        - type: object
          required: [user]
          properties:
            user:
              $ref: "#/components/schemas/AuthenticatedUser"

    AuthenticatedUser:
      type: object
      required: [id]
      properties:
        id:
          type: string
          format: uuid
          description: UUIDv7 account identifier.

    JsonWebKeySet:
      type: object
      required: [keys]
      properties:
        keys:
          type: array
          items:
            $ref: "#/components/schemas/JsonWebKey"

    JsonWebKey:
      type: object
      required: [kty, use, alg, kid, crv, x, y]
      properties:
        kty:
          type: string
          enum: [EC]
        use:
          type: string
          enum: [sig]
        alg:
          type: string
          enum: [ES256]
        kid:
          type: string
        crv:
          type: string
          enum: [P-256]
        x:
          type: string
        y:
          type: string

    Error:
      type: object
      description: >-
        Стабильная публичная ошибка. Для валидации используются коды
        INVALID_EMAIL_FORMAT, EMAIL_TOO_LONG, EMAIL_LOCAL_PART_UNSUPPORTED,
        EMAIL_DOMAIN_NOT_SUPPORTED, PASSWORD_TOO_SHORT, PASSWORD_TOO_LONG,
        PASSWORD_INVALID_CHARACTERS, INVALID_LOCALE, INVALID_DEVICE_ID,
        INVALID_REQUEST, INVALID_CREDENTIALS, INVALID_REFRESH_TOKEN,
        INVALID_VERIFICATION_TOKEN, REGISTRATION_ATTEMPT_CONFLICT и PAYLOAD_TOO_LARGE.
      required: [code, message]
      properties:
        code:
          type: string
          example: INVALID_REQUEST
        message:
          type: string
          example: Invalid request.
        details:
          type: object
          additionalProperties: true
        requestId:
          type: string
          format: uuid
